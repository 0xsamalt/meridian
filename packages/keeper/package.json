{
  "name": "@meridian/keeper",
  "version": "0.1.0",
  "private": true,
  "description": "Off-chain AI keeper — reads Nansen + Elfa signals, calls vault.rebalance()",
  "main": "dist/index.js",
  "scripts": {
    "dev": "ts-node src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "viem": "^2.0.0",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "ts-node": "^10.0.0",
    "@types/node": "^20.0.0"
  }
}
